---
milestone: v0.1
milestone_name: Runtime hygiene wedge (the lovable MVP)
status: gaps_found
gap_kind: human_needed_handoff
audited_at: 2026-04-29
phases_audited: 4
phases_passed: 3
phases_partial: 1
requirements_total: 28
requirements_satisfied: 27
requirements_deferred: 1
integration_check: skipped (cross-phase wiring enforced by constraint test suite C2/C4/C6 at HEAD)
---

# v0.1 Milestone Audit

## Status determination

**Final: `gaps_found` (handoff-only — no codebase work remains).**

Phase-level verifications:

| Phase | Verifier verdict | Reviewer verdict | Fix-pass | Notes |
|---|---|---|---|---|
| 1 | passed | reviewed; all critical+warning fixed | applied | 28/28 tests; 11 reqs (CORE-01..10 + HOOK-11) |
| 2 | passed | 11/11 critical+warning fixed | applied | 51/51 tests; 6 reqs (HOOK-01..06); D-15 hook self-bench gate enforced |
| 3 | passed | 6/6 critical+warning fixed; 5 info deferred | applied | 80/80 tests; **all 6 hard constraints C1..C6 enforced** (C3, C4 newly via tests/cli_dep_boundary.rs); 4 reqs (HOOK-07..10) |
| 4 | **human_needed** (PARTIAL) | PASS-WITH-WARNINGS | 3/4 fixed; WR-02 deferred + rationale | 80/80 tests; 6 reqs (DIST-01, 03..07); DIST-02 deferred to v0.1.x per AMENDMENT 2026-04-29 |

Phase 4's `human_needed` PARTIAL status surfaces 3 maintainer-only handoff items (per `RC1-CHECKLIST.md`):
1. `gh repo create Sinderella/holt --public --source . --remote origin --push`
2. `tools/setup-labels.sh` apply (depends on #1)
3. `git tag v0.1.0-rc.1 && git push origin v0.1.0-rc.1` (depends on #1)

These are infrastructure-only — no codebase gaps. The audit treats them as `gaps_found` per the workflow rubric, but the gap kind is `human_needed_handoff` (documented end-to-end with rollback in RC1-CHECKLIST.md).

## Requirements coverage (28 v1 REQ-IDs, 3-source cross-reference)

Sources cross-checked:
- A: `.planning/REQUIREMENTS.md` traceability table (lines 137–167)
- B: each phase's `*-VERIFICATION.md` requirements section
- C: each plan's `*-SUMMARY.md` frontmatter `requirements:` field

| REQ-ID | Phase | A: REQUIREMENTS.md | B: VERIFICATION.md | C: SUMMARY.md frontmatter | Final |
|---|---|---|---|---|---|
| CORE-01..10 | 1 | `[x]` (assumed; all phases pre-marked) | passed | listed (Plan 01-01..03) | **satisfied** |
| HOOK-11 | 1 | `[x]` | passed | listed | **satisfied** |
| HOOK-01..06 | 2 | `[x]` | passed | listed (Plan 02-01..02) | **satisfied** |
| HOOK-07..10 | 3 | `[x]` | passed | listed (Plan 03-01..02) | **satisfied** |
| DIST-01 | 4 | `[ ]` (still unchecked) | structurally verified | listed (Plan 04-01) | **satisfied** (update checkbox) |
| DIST-02 | 4 | `[~]` (deferred — strikethrough) | DEFERRED via AMENDMENT 2026-04-29 | listed as deferred (Plan 04-02) | **deferred** (out of v0.1 scope) |
| DIST-03 | 4 | `[ ]` | structurally verified; live-resolve pending rc.1 tag-push | listed (Plan 04-01) | **satisfied** (verification handed off to RC1-CHECKLIST.md §7) |
| DIST-04 | 4 | `[ ]` | passed | listed (Plan 04-02) | **satisfied** |
| DIST-05 | 4 | `[ ]` | passed | listed (Plan 04-01) | **satisfied** |
| DIST-06 | 4 | `[ ]` | passed | listed (Plan 04-02) | **satisfied** |
| DIST-07 | 4 | `[ ]` | structurally verified; live `gh label list` pending repo creation | listed (Plan 04-02; Task 5 routed to RC1-CHECKLIST step 3) | **satisfied** (label apply handed off) |

**Totals:**
- Satisfied: 27/28
- Deferred (not in v0.1 scope): 1/28 (DIST-02 — Homebrew tap; trigger criteria documented)
- Unsatisfied: 0/28
- Orphans: 0 (every REQ-ID is referenced in at least one phase's VERIFICATION.md)

Per the workflow's status-determination matrix: 0 unsatisfied requirements → no FAIL gate triggered. The `gaps_found` status comes from Phase 4's `human_needed` verifier verdict, not from requirement coverage.

## Cross-phase integration

**Skipped agent spawn — covered by existing constraint tests at HEAD:**

| Constraint | Test |
|---|---|
| C1 — Always pipe stdio when spawning supervised processes | `crates/holt-supervisor/tests/chokepoint_audit.rs` |
| C2 — `holt-render` MUST NOT depend on `holt-supervisor` | `tests/architecture_dag.rs` |
| C3 — `~/.claude/settings.json` mutation atomicity | `crates/holt-cli/tests/install_hooks_concurrent.rs` + `install_hooks_sigkill.rs` |
| C4 — JSONC handling lives ONLY in `holt-cli` | `tests/cli_dep_boundary.rs` |
| C5 — Reader treats stale-or-corrupt heartbeat as missing | `crates/holt-schemas/tests/reader_contract.rs` |
| C6 — Render path never reads `breaches.log` or `timings.jsonl` | `crates/holt-cli/tests/render_path_no_read.rs` |

All 6 constraint tests pass at HEAD (`5b9ffdb`). The wiring between phases (Phase 1 schemas keystone → Phase 2 hook write side → Phase 3 install-hooks → Phase 4 dist artifact) is exercised by:
- `crates/holt-cli/tests/version_smoke.rs` — Phase 1 `--version` plumbing → Phase 4 `CARGO_PKG_VERSION` parity
- `crates/holt-hooks/tests/handle_event_smoke.rs` — Phase 2 hook → Phase 1 atomic_write + read_heartbeat keystone
- `crates/holt-cli/tests/install_hooks_*.rs` — Phase 3 JSONC merge → Phase 1 atomic_write
- The 80/80 workspace test count proves the wiring holds at HEAD.

## Tech debt + deferred items

| Item | Phase | Trigger to revisit |
|---|---|---|
| DIST-02 Homebrew tap | 4 | ≥3 macOS Gatekeeper friction issues OR Apple Developer Program enrollment |
| D-08 branch 2 (cwd_label `git rev-parse` heuristic) | 2 | v1.0 — needs `holt doctor` seam |
| Code signing (macOS notarization, Windows EV cert) | 4 | v0.1.x+; trigger-gated |
| Cargo install via crates.io | 4 | v0.5+; depends on internal-library API stability |
| `cargo binstall holt --version 0.1.0-rc.1 --dry-run` end-to-end probe | 4 | post-rc.1 tag-push (RC1-CHECKLIST §7) |
| `gh label list --repo Sinderella/holt` confirmation | 4 | post-`gh repo create` + `tools/setup-labels.sh` (RC1-CHECKLIST §3) |
| Re-signing the 99 history-rewrite-rewritten commits | meta | accepted as unsigned; new commits sign normally |

## Performance gates at HEAD

| Gate | Target | Actual |
|---|---|---|
| `holt --self-bench` p95 | < 20ms | 0us |
| `holt --self-bench-hook PreToolUse` p95 | < 20ms | 5874us |
| `holt install-hooks --dry-run` p95 | < 500ms | within budget |
| Workspace test count | n/a | 80/80 green |
| `dist plan` | exit 0 | 4 platform tarballs + sha256 sidecars + sha256.sum aggregate |
| MSRV 1.87 build matrix | green on Linux+macOS REQUIRED | configured (CI runs on PRs) |

## Recommendation

**Proceed to milestone-complete.** The `gaps_found` status reflects deferred-and-handed-off items, not codebase defects. Phase 4's `RC1-CHECKLIST.md` is the canonical handoff document for the maintainer's three remaining steps. After tag-push, the `cargo binstall` dry-run probe + `gh release view v0.1.0-rc.1` close the verification loop.

Cross-reference: `.planning/MILESTONE-V0.1-AUDIT.md` (the inline audit committed earlier in `5b9ffdb`) — same content with broader narrative; this file uses the workflow's expected schema/filename for downstream `gsd-complete-milestone` consumption.
