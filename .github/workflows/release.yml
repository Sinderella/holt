# This file was autogenerated by dist: https://axodotdev.github.io/cargo-dist
#
# Copyright 2022-2024, axodotdev
# SPDX-License-Identifier: MIT or Apache-2.0
#
# CI that:
#
# * checks for a Git Tag that looks like a release
# * builds artifacts with dist (archives, installers, hashes)
# * uploads those artifacts to temporary workflow zip
# * on success, uploads the artifacts to a GitHub Release
#
# Note that the GitHub Release will be created with a generated
# title/body based on your changelogs.

name: Release
permissions:
  "contents": "write"

# This workflow is generated by `dist init` (D-01 / 04-CONTEXT.md). Do not
# hand-edit unless absolutely necessary — prefer regenerating via `dist generate
# --mode ci` after temporarily commenting out `allow-dirty = ["ci"]` in
# dist-workspace.toml.
# Holt-specific customizations on top of the generated baseline:
#   - Tag-push pattern is `v*` (D-11; broader than dist's default `'**[0-9]+.[0-9]+.[0-9]+*'`
#     so the locked v0.1.0-rc.1 SemVer pre-release tag in plan 04-02 D-10 is
#     guaranteed to match. dist's default pattern accepts rc tags too, but the
#     simpler `v*` is documented per D-11).
#   - Windows matrix entry has `continue-on-error: ${{ contains(matrix.targets,
#     'x86_64-pc-windows-msvc') }}` (D-03; Unix-tier-1, Windows best-effort
#     per docs/02-scope.md). The downstream `host` job's
#     `needs.build-local-artifacts.result == 'success'` gate works because
#     per-instance continue-on-error masks failure to success at the needs
#     level.
#   - Homebrew tap publish-job removed 2026-04-29 — D-04 revised: tap deferred
#     to v0.1.x. Re-adding requires (a) `installers = [..., "homebrew"]` +
#     `tap = "<user>/homebrew-holt"` + `formula = "holt"` + `publish-jobs =
#     ["homebrew"]` in dist-workspace.toml, (b) `dist generate --mode ci` to
#     re-emit the `publish-homebrew-formula` job, (c) re-applying the Windows
#     continue-on-error hand-edit per the routing block above.
#   - SHA-256 checksums emitted alongside every artifact (D-12; dist 0.31
#     default; verified via `dist plan` reporting 4 .sha256 sidecars + 1
#     sha256.sum aggregate + 1 source.tar.gz.sha256).
#   - Per-target `--version` parity check (D-14): assert the just-built binary's
#     --version output contains ${GITHUB_REF_NAME#v} for the matrix host. Plan
#     04-02 added this step; failure fails the release for that target only
#     (the Windows continue-on-error masking still applies). The smoke test
#     `crates/holt-cli/tests/version_smoke.rs` (D-13) is the local complement
#     and has already been replaced to assert against
#     `concat!("holt ", env!("CARGO_PKG_VERSION"))`. NOTE: this step is also
#     wiped by `dist generate --mode ci` — re-apply per the same playbook as
#     the Windows continue-on-error edit (allow-dirty = ["ci"] in
#     dist-workspace.toml protects the file from being overwritten on
#     accidental regeneration, but a deliberate regen DOES require re-apply).

# This task will run whenever you push a git tag that looks like a version
# like "1.0.0", "v0.1.0-prerelease.1", "my-app/0.1.0", "releases/v1.0.0", etc.
# Various formats will be parsed into a VERSION and an optional PACKAGE_NAME, where
# PACKAGE_NAME must be the name of a Cargo package in your workspace, and VERSION
# must be a Cargo-style SemVer Version (must have at least major.minor.patch).
#
# If PACKAGE_NAME is specified, then the announcement will be for that
# package (erroring out if it doesn't have the given version or isn't dist-able).
#
# If PACKAGE_NAME isn't specified, then the announcement will be for all
# (dist-able) packages in the workspace with that version (this mode is
# intended for workspaces with only one dist-able package, or with all dist-able
# packages versioned/released in lockstep).
#
# If you push multiple tags at once, separate instances of this workflow will
# spin up, creating an independent announcement for each one. However, GitHub
# will hard limit this to 3 tags per commit, as it will assume more tags is a
# mistake.
#
# If there's a prerelease-style suffix to the version, then the release(s)
# will be marked as a prerelease.
on:
  pull_request:
  push:
    # D-11: tag-push pattern normalized to `v*` (broader than dist's default
    # `'**[0-9]+.[0-9]+.[0-9]+*'`). Both patterns accept v0.1.0-rc.1, but `v*`
    # is the canonical Rust-ecosystem release flow and is what 04-CONTEXT.md
    # D-11 names. Hand-edit covered by `allow-dirty = ["ci"]` in
    # dist-workspace.toml.
    tags:
      - 'v*'

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ !github.event.pull_request && github.ref_name || '' }}
      tag-flag: ${{ !github.event.pull_request && format('--tag={0}', github.ref_name) || '' }}
      publishing: ${{ !github.event.pull_request }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install dist
        # we specify bash to get pipefail; it guards against the `curl` command
        # failing. otherwise `sh` won't catch that `curl` returned non-0
        shell: bash
        run: "curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v0.31.0/cargo-dist-installer.sh | sh"
      - name: Cache dist
        uses: actions/upload-artifact@v6
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/dist
      # sure would be cool if github gave us proper conditionals...
      # so here's a doubly-nested ternary-via-truthiness to try to provide the best possible
      # functionality based on whether this is a pull_request, and whether it's from a fork.
      # (PRs run on the *source* but secrets are usually on the *target* -- that's *good*
      # but also really annoying to build CI around when it needs secrets to work right.)
      - id: plan
        run: |
          dist ${{ (!github.event.pull_request && format('host --steps=create --tag={0}', github.ref_name)) || 'plan' }} --output-format=json > plan-dist-manifest.json
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@v6
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
    if: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null && (needs.plan.outputs.publishing == 'true' || fromJson(needs.plan.outputs.val).ci.github.pr_run_mode == 'upload') }}
    strategy:
      fail-fast: false
      # Target platforms/runners are computed by dist in create-release.
      # Each member of the matrix has the following arguments:
      #
      # - runner: the github runner
      # - dist-args: cli flags to pass to dist
      # - install-dist: expression to run to install dist on the runner
      #
      # Typically there will be:
      # - 1 "global" task that builds universal installers
      # - N "local" tasks that build each platform's binaries and platform-specific installers
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    # D-03: Windows is best-effort at v0.1 — a Windows build failure must NOT
    # block the Linux + macOS-x64 + macOS-arm64 release. Setting
    # `continue-on-error` per matrix instance via expression keeps the Linux
    # and macOS instances strictly required (continue-on-error stays false for
    # them) while the Windows instance is tolerated. The downstream `host` job
    # gates on `needs.build-local-artifacts.result == 'success'`; per-instance
    # `continue-on-error: true` masks per-instance failure to success at the
    # needs-level, so the host job runs and publishes the Linux + macOS
    # tarballs even if Windows failed. Linux arm64 + Windows arm64 are not in
    # the matrix at v0.1 per docs/02-scope.md. NOTE: this hand-edit is wiped
    # by `dist generate --mode ci` — see dist-workspace.toml's allow-dirty
    # comment for the re-application playbook.
    continue-on-error: ${{ contains(matrix.targets, 'x86_64-pc-windows-msvc') }}
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install Rust non-interactively if not already installed
        if: ${{ matrix.container }}
        run: |
          if ! command -v cargo > /dev/null 2>&1; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          fi
      - name: Install dist
        run: ${{ matrix.install_dist.run }}
      # Get the dist-manifest
      - name: Fetch local artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        run: |
          ${{ matrix.packages_install }}
      - name: Build artifacts
        run: |
          # Actually do builds and make zips and whatnot
          dist build ${{ needs.plan.outputs.tag-flag }} --print=linkage --output-format=json ${{ matrix.dist_args }} > dist-manifest.json
          echo "dist ran successfully"
      - id: cargo-dist
        name: Post-build
        # We force bash here just because github makes it really hard to get values up
        # to "real" actions without writing to env-vars, and writing to env-vars has
        # inconsistent syntax between shell and powershell.
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          dist print-upload-files-from-manifest --manifest dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: Verify --version output matches the tag (D-14)
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        run: |
          set -euo pipefail
          # Locate the freshly-built binary. dist 0.31 stages the binary under
          # target/<triple>/release/ (or target/release/ for the host triple).
          # Restrict the find to the canonical release path (and explicitly to
          # the holt or holt.exe basename) so we don't pick up a build artifact
          # from a debug profile or an unrelated subdir if the build layout
          # ever changes — WR-03 robustness fix.
          BIN=$(find target -type f \
                  \( -path '*/release/holt' -o -path '*/release/holt.exe' \) \
                  -perm -u+x 2>/dev/null | head -1)
          if [ -z "$BIN" ]; then
            echo "FAIL: could not locate built holt binary under target/*/release/ or target/release/"
            find target -maxdepth 4 -type f -name 'holt*' 2>/dev/null || true
            exit 1
          fi
          echo "binary: $BIN"
          VERSION_OUT="$($BIN --version)"
          echo "stdout: $VERSION_OUT"
          # Strip leading 'v' from tag: refs/tags/v0.1.0-rc.1 -> 0.1.0-rc.1.
          EXPECTED="${GITHUB_REF_NAME#v}"
          echo "expected tag: $EXPECTED"
          # The binary prints `holt <version>` (concat!("holt ", env!("CARGO_PKG_VERSION"))).
          # Assert the printed version contains the expected tag.
          echo "$VERSION_OUT" | grep -qF "$EXPECTED" || (
            echo "FAIL: holt --version stdout (\"$VERSION_OUT\") does not contain tag (\"$EXPECTED\")"
            echo "Did the maintainer forget to bump Cargo.toml's version before tagging?"
            exit 1
          )
          echo "OK: --version parity confirmed (binary version contains $EXPECTED)"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@v6
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    needs:
      - plan
      - build-local-artifacts
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install cached dist
        uses: actions/download-artifact@v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Get all the local artifacts for the global tasks to use (for e.g. checksums)
      - name: Fetch local artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        run: |
          dist build ${{ needs.plan.outputs.tag-flag }} --output-format=json "--artifacts=global" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          jq --raw-output ".upload_files[]" dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@v6
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
  # Determines if we should publish/announce
  host:
    needs:
      - plan
      - build-local-artifacts
      - build-global-artifacts
    # Only run if we're "publishing", and only if plan, local and global didn't fail (skipped is fine)
    if: ${{ always() && needs.plan.result == 'success' && needs.plan.outputs.publishing == 'true' && (needs.build-global-artifacts.result == 'skipped' || needs.build-global-artifacts.result == 'success') && (needs.build-local-artifacts.result == 'skipped' || needs.build-local-artifacts.result == 'success') }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.host.outputs.manifest }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive
      - name: Install cached dist
        uses: actions/download-artifact@v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Fetch artifacts from scratch-storage
      - name: Fetch artifacts
        uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: host
        shell: bash
        run: |
          dist host ${{ needs.plan.outputs.tag-flag }} --steps=upload --steps=release --output-format=json > dist-manifest.json
          echo "artifacts uploaded and released successfully"
          cat dist-manifest.json
          echo "manifest=$(jq -c "." dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@v6
        with:
          # Overwrite the previous copy
          name: artifacts-dist-manifest
          path: dist-manifest.json
      # Create a GitHub Release while uploading all files to it
      - name: "Download GitHub Artifacts"
        uses: actions/download-artifact@v7
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Cleanup
        run: |
          # Remove the granular manifests
          rm -f artifacts/*-dist-manifest.json
      - name: Create GitHub Release
        env:
          PRERELEASE_FLAG: "${{ fromJson(steps.host.outputs.manifest).announcement_is_prerelease && '--prerelease' || '' }}"
          ANNOUNCEMENT_TITLE: "${{ fromJson(steps.host.outputs.manifest).announcement_title }}"
          ANNOUNCEMENT_BODY: "${{ fromJson(steps.host.outputs.manifest).announcement_github_body }}"
          RELEASE_COMMIT: "${{ github.sha }}"
        run: |
          # Write and read notes from a file to avoid quoting breaking things
          echo "$ANNOUNCEMENT_BODY" > $RUNNER_TEMP/notes.txt

          gh release create "${{ needs.plan.outputs.tag }}" --target "$RELEASE_COMMIT" $PRERELEASE_FLAG --title "$ANNOUNCEMENT_TITLE" --notes-file "$RUNNER_TEMP/notes.txt" artifacts/*

  # D-04 (revised 2026-04-29): publish-homebrew-formula job removed; Homebrew tap
  # deferred to v0.1.x. v0.1 ships via shell + powershell installer scripts
  # plus prebuilt GitHub-release tarballs only.

  announce:
    needs:
      - plan
      - host
    # "host" must run to completion, no skipping allowed.
    if: ${{ always() && needs.host.result == 'success' }}
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
          submodules: recursive
